@model IEnumerable<BoardCockpit.Models.Report>

@{
    ViewBag.Title = "Index";
}
@section CustomJS{
    <!-- DataTables CSS -->
    <link rel="stylesheet" type="text/css" href="//cdn.datatables.net/1.10.2/css/jquery.dataTables.css">

    <!-- jQuery -->
    @*<script type="text/javascript" charset="utf8" src="//code.jquery.com/jquery-1.10.2.min.js"></script>*@

    <!-- DataTables -->
    <script type="text/javascript" charset="utf8" src="//cdn.datatables.net/1.10.2/js/jquery.dataTables.js"></script>

    <script>
        $(document).ready(function () {
            $('#table_id').DataTable({
                searching: true,
                ordering: true
            });
        });
    </script>
}
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="display" id="table_id">
    <thead>
        <tr>
            <th style="text-align:left">
                @Html.DisplayNameFor(model => model.AccordingToYearEnd)
            </th>
            <th style="text-align:left">
                @Html.DisplayNameFor(model => model.ReportType)
            </th>
            <th style="text-align:left">
                @Html.DisplayNameFor(model => model.XbrlContextRef)
            </th>
            <th style="text-align:left">
                Aktionen
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.AccordingToYearEnd)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ReportType)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.XbrlContextRef)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.ReportID }) |
                    @Html.ActionLink("Details", "Details", new { id = item.ReportID }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.ReportID })
                </td>
            </tr>
        }
</tbody>

</table>
