@model BoardCockpit.Models.Unit

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <form>
        <fieldset>
            <legend>Unit</legend>
            <table>
                <tr>
                    <td><strong>@Html.DisplayNameFor(model => model.XbrlUnitID)</strong></td>
                    <td>@Html.DisplayFor(model => model.XbrlUnitID)</td>
                </tr>
                <tr>
                    <td><strong>@Html.DisplayNameFor(model => model.Value)</strong></td>
                    <td>@Html.DisplayFor(model => model.Value)</td>
                </tr>
            </table>
        </fieldset>
    </form>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.UnitId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
